function UnlockAnimation({ label = 'Conquista desbloqueada' }) {
  return (
    <span className="unlock-animation" aria-label={label}>
      <span />
      <span />
      <span />
    </span>
  );
}

export default UnlockAnimation;
